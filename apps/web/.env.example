# `pnpm db:start` の出力に表示される API URL と anon key を設定する。
# VITE_ 接頭辞の変数はブラウザのバンドルに埋め込まれる。
# anon key は RLS 前提で公開される鍵なのでここに置いてよい。
# service_role キーは絶対にここに置かない（Cloudflare Workers の secret として扱う）。
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=
